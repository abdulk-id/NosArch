# For setting the default terminal
# It reads from ~/.config/xdg-terminals.list
TERMINAL=xdg-terminal-exec

# For setting the default editor
EDITOR=nano

# For setting the default graphical editor
VISUAL="zed --wait"

FILE_MANAGER=nautilus
