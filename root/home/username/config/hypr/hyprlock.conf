$font = JetBrainsMono Nerd Font
$color = rgba(%BACKGROUND%FF)
$inner_color = rgba(%BACKGROUND%A8)
$outer_color = rgba(%COLOR5%FF)
$font_color = rgba(%FOREGROUND%FF)
$placeholder_color = rgba(%FOREGROUND%B3)
$check_color = rgba(%COLOR6%FF)

general {
    hide_cursor = true
    ignore_empty_input = true
    fail_timeout = 1500 # 1.5 seconds
}

auth {
    fingerprint:enabled = false
}

background {
    monitor =
    color = $color
    path = ~/.local/share/current-theme/current-background.png
    blur_passes = 2
}

animations {
    enabled = true
    animation = fadeIn, 1, 2, linear
    animation = fadeOut, 1, 2, linear
}

input-field {
    monitor =
    size = 300, 50
    position = 0, 100
    halign = center
    valign = bottom

    inner_color = $inner_color
    outer_color = $outer_color
    outline_thickness = 3

    font_family = $font
    font_color = $font_color

    placeholder_text = Enter Password
    check_color = $check_color
    fail_text = $FAIL ($ATTEMPTS)

    rounding = 10
    shadow_passes = 0
    fade_on_empty = false
}

# User Icon
label {
    text = <span>  </span>

    monitor =
    position = -8, 200
    halign = center
    valign = bottom

    font_family = $font
    font_size = 35
    color = $font_color
}

# Username
label {
    text = $USER

    monitor =
    position = 0, 165
    halign = center
    valign = bottom

    font_family = $font
    font_size = 15
    color = $font_color
}

# Date
label {
    text = cmd[update:60000] date +"%A, %d %B %Y" # update every 60 seconds

    monitor =
    position = 0, -185
    halign = center
    valign = top

    font_family = $font
    font_size = 13
    color = $font_color
}

# Time
label {
    text = $TIME12

    monitor =
    position = 0, -100
    halign = center
    valign = top

    font_family = $font
    font_size = 50
    color = $font_color
}

# Battery indicator
label {
    text = cmd[update:20000] echo -e "󰁹 $(cat /sys/class/power_supply/BAT*/capacity)%"

    monitor =
    position = -30, -30
    halign = right
    valign = top

    font_family = $font
    font_size = 10
    color = $font_color

    shadow_passes = 0
}
