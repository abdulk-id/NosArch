#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

# Use if not using a greeter service
#if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then
#    if uwsm check may-start; then
#        exec uwsm start hyprland.desktop
#    fi
#fi
